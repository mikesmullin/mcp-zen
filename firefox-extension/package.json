{
  "name": "mcp-zen-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/background.js",
  "scripts": {
    "build": "esbuild background.js --bundle --format=iife --outfile=dist/background.js --loader:.yaml=text && esbuild options.js --bundle --format=iife --outfile=dist/options.js --loader:.yaml=text && esbuild page-runtime.js --bundle --format=iife --outfile=dist/page-runtime.js && esbuild console-hook.js --bundle --format=iife --outfile=dist/console-hook.js"
  },
  "license": "MIT",
  "description": "Zen/Firefox extension that lets the mcp-zen MCP server remote-control this browser",
  "dependencies": {
    "dom-accessibility-api": "^0.7.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@mcp-zen/common": "*",
    "esbuild": "^0.25.1"
  }
}
